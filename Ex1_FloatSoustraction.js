let a = 0.2;
let b = 0.3;

let difference = b - a;
if (difference === 0.1) {
    console.log("La différence est égale à 0.1 : " + difference);
} else {
    console.warn("La différence n'est pas égale à 0.1 ! : " + difference);
}